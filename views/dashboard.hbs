{{>page/menu active="dashboard"}}
{{#if stations_exist}}
{{>station-list}}
{{/if}}
{{>add-station}}
{{#if userIsAdmin}}
    {{#if deleted_station_exist}}
        {{>station-list-deleted}}
    {{/if}}
{{/if}}
{{>page/footer}}