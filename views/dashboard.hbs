{{>page/menu active="dashboard"}}
{{>map}}
{{#if stationsExist}}
{{>station-list}}
{{/if}}
    {{#if userIsAdmin}}
    {{>add-station}}
        {{#if deletedStationsExist}}
            {{>station-list-deleted}}
        {{/if}}
    {{/if}}
{{>page/footer}}