<nav class="navbar mb-6 is-align-items-baseline">
  <div class="navbar-brand">
    {{>page/brand}}
  </div>
   <span id="cityStatus">TODO: in sunny Co.Waterford</span>
  <div class="navbar-menu" id="navMenu">
    <div class="navbar-end">
      <div class="navbar-item">
        <div class="buttons">
          {{#if userLoggedIn}}
          <a id="dashboard" class="button" href="/dashboard"> Dashboard </a>
          {{/if}}
          <a id="about" class="button" href="/about"> About </a>
          {{#unless userLoggedIn}}
          <a id="login" class="button" href="/login"> Login </a>
          <a id="signup" class="button" href="/signup"> Sign Up </a>
          {{/unless}}
          {{#if userLoggedIn}}
          <a id="account" class="button" href="/account"> Account </a>
          <a id="logout" class="button" href="/logout"> Logout </a>
          {{/if}}
          <a id="test" class="button" href="/test"> Test </a>
        </div>
      </div>
    </div>
  </div>
</nav>

<script>
  try { document.getElementById("{{active}}").classList.add("is-primary"); } catch { console.log("menu.hbs: no active menu")}
  try { document.getElementById("cityStatus").innerHTML = `in ${codeFriendlyName} ${stationCounty}`; } catch { console.log("menu.hbs: no city")} //TODO: make "in" "sunny/rainy/etc." "Co. Galway/Co.Waterford etc."
</script>