<style>
    .card {
  margin-bottom: 1rem;
  gap: 0.5rem;
    }
  .card-header-icon{
    position: relative;
    pointer-events: none;

}
</style>

<section class="is-flex is-justify-content-center is-justify-content-space-around" style="margin-bottom: 2rem;">
        <div class="p-2">
            <div class="is-flex is-flex-direction-row">
            <div class="card-header-title">Location<br>{{currentStation.city}}</div>
            <div class="card-header-icon">{{>icons/location}}</div>
            </div>
            <div class="card-content">
                <p>Longitude: {{currentStation.longitude}}</p>
                <p>Latitude: {{currentStation.latitude}}</p>
            </div>
        </div>

        <div class="p-2">
            <div class="is-flex is-flex-direction-row">
            <div class="card-header-title">Weather<br>{{currentStation.city}}</div>
            <div class="card-header-icon">{{>icons/location}}</div>
        </div>
            <div class="card-content">
                <p>Longitude: {{currentStation.longitude}}</p>
                <p>Latitude: {{currentStation.latitude}}</p>
            </div>
        </div>

        <div class="p-2">
            <div class="is-flex is-flex-direction-row">
            <div class="card-header-title">Temp<br>{{currentStation.city}}</div>
            <div class="card-header-icon">{{>icons/temperature}}</div>
            </div>
            <div class="card-content">
                <p>Longitude: {{currentStation.longitude}}</p>
                <p>Latitude: {{currentStation.latitude}}</p>
            </div>
        </div>

        <div class="p-2">
            <div class="is-flex is-flex-direction-row">
            <div class="card-header-title">Wind<br>{{currentStation.city}}</div>
            <div class="card-header-icon">{{>icons/wind}}</div>
        </div>
            <div class="card-content">
                <p>Speed: {{currentStation.longitude}}</p>
                <p>Direction: {{currentStation.latitude}}</p>
            </div>
        </div>

        <div class="p-2">
            <div class="is-flex is-flex-direction-row">
            <div class="card-header-title">Pressure<br>{{currentStation.city}}</div>
            <div class="card-header-icon">{{>icons/pressure}}</div>
            </div>
            <div class="card-content">
                <p>Longitude: {{currentStation.longitude}}</p>
                <p>Latitude: {{currentStation.latitude}}</p>
            </div>
        

    </div>
</section>